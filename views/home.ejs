<%- include("partials/header") -%>
  <h1>Home</h1>
  <p> <%=startingContent %> </p>

  <% posts.forEach(item => { %>
    <h1>
      <%= item.title %>
    </h1>
    <p>
      <%= item.body %>
    </p>
  <% }) %>
  
<%- include("partials/footer") -%>
